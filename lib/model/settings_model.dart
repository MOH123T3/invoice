import 'package:autorepair/imports.dart';

class SettingsModel {
  final String? title;
  final IconData? icon;
  final Color? bgColor;
  final Color? textColor;

  SettingsModel({this.title, this.icon, this.bgColor, this.textColor});
}
